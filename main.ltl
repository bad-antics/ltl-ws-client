// ltl-ws-client/main.ltl
//
// RFC 6455 WebSocket client with permessage-deflate.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-ws-client v0.1.0")
    io::println("RFC 6455 WebSocket client with permessage-deflate.")
    0
}
